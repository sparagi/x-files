instance creation
initializedInstance
	"Answer an XMLNamespaceResolver whose parent is the default XMLNamespaceResolver, i.e. the one which contains mappings from all predefined prefixes to the corresponding XMLNamespaces."
	
	^Default child